rules_version = '2';

// TEMPORARY test-mode rules — open for demo/testing since Module 1 (Auth)
// is still mocked and no real Firebase Auth UIDs exist yet. Replace with
// the auth-scoped version below before any real user data goes in.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// Intended real rules, to restore once Module 1 (Auth) is wired up:
//
// service cloud.firestore {
//   match /databases/{database}/documents {
//     match /live_sessions/{sessionId} {
//       allow read: if request.auth != null &&
//         (request.auth.uid == resource.data.ownerId ||
//          request.auth.uid in resource.data.sharedWithUserIds);
//
//       allow create: if request.auth != null &&
//         request.auth.uid == request.resource.data.ownerId;
//
//       allow update: if request.auth != null &&
//         request.auth.uid == resource.data.ownerId;
//
//       allow delete: if false;
//     }
//
//     // Module 2 — Contacts. Module 2 now signs every device in with
//     // Firebase Anonymous Auth (see lib/services/auth_service.dart), so
//     // request.auth.uid is real and stable even before Module 1 ships
//     // real phone verification — this rule can be enabled today.
//     match /users/{userId}/contacts/{contactId} {
//       allow read, write: if request.auth != null &&
//         request.auth.uid == userId;
//     }
//   }
// }
//
// Left as a suggestion rather than switched on here because the root
// wildcard rule above is still relied on by other modules that aren't
// auth-gated yet — flipping it is a whole-team decision, not a Module 2
// one. Ping the team lead when other modules are ready to move off it.
