// File generated by FlutterFire CLI, then hand-edited.
// ignore_for_file: type=lint
import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;
import 'package:flutter/foundation.dart' show kIsWeb;

/// Default [FirebaseOptions] for the SafeHer Admin Dashboard.
///
/// PENDING SETUP: this app talks to the *same* Firebase project as the
/// SafeHer mobile app (`safeher-206b6`), but that project has never had a
/// **Web** app registered on it — only Android/iOS. Until it does, every
/// value below is a placeholder and `Firebase.initializeApp()` will throw
/// on launch.
///
/// To finish this (one of):
///  1. Firebase Console → safeher-206b6 → Project settings → Add app → Web
///     → copy the generated `firebaseConfig` object → paste its values in
///     below, or
///  2. Run `firebase login` yourself, then `flutterfire configure` from
///     this project's root to generate this file for real.
///
/// See README.md for the rest of the one-time setup (Firestore rules +
/// bootstrapping the first admin account).
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    if (kIsWeb) return web;
    throw UnsupportedError(
      'DefaultFirebaseOptions are only configured for web — '
      'the admin dashboard is a web-only app.',
    );
  }

  static const FirebaseOptions web = FirebaseOptions(
    apiKey: 'REPLACE_WITH_WEB_API_KEY',
    appId: 'REPLACE_WITH_WEB_APP_ID',
    messagingSenderId: '831160190675',
    projectId: 'safeher-206b6',
    authDomain: 'safeher-206b6.firebaseapp.com',
    storageBucket: 'safeher-206b6.firebasestorage.app',
  );
}
